# Local dev: leave this unset — the Next.js rewrite proxies /api/* to localhost:4000.
#
# Production (Vercel): set this to your deployed backend URL so the browser calls it directly.
# NEXT_PUBLIC_API_BASE=https://your-backend.onrender.com
